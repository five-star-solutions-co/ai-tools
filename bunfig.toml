[install]
exact = true

[test]
# Unit script scopes paths; live IT is test/integration/**/*.live.test.ts
# (run via test:integration / IDE after integration:up — not default `bun test` paths).
pathIgnorePatterns = ["**/*.integration.test.ts"]
