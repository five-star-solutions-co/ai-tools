diff --git a/node_modules/@office-open/core/.bun-tag-780560b7edcbe95a b/.bun-tag-780560b7edcbe95a
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/util-Tq9PSjK0.mjs b/dist/util-Tq9PSjK0.mjs
index 0b3f7132e789de375511d449e7e9ad2f378d7038..05884f5c2a600d173ed8fbdd6b9c01a4f65ee121 100644
--- a/dist/util-Tq9PSjK0.mjs
+++ b/dist/util-Tq9PSjK0.mjs
@@ -39,21 +39,13 @@ function computeCrc32(data) {
 	for (const byte of data) crc = CRC_TABLE[(crc ^ byte) & 255] ^ crc >>> 8;
 	return ~crc;
 }
+// Upstream used top-level `await import("node:zlib")` for optional native compression.
+// That breaks Bun → CJS lambda bundlers ("await can only be used inside an async function").
+// Leave natives unset; fflate paths below always work (same fallback as when zlib is unavailable).
 let _nativeDeflate;
 let _nativeDeflateAsync;
 let _nativeInflate;
 let _nativeCrc32;
-try {
-	const zlib = await import("node:zlib");
-	if (typeof zlib.deflateRawSync !== "function") throw new Error("no native deflate");
-	_nativeDeflate = (data, level) => zlib.deflateRawSync(data, { level });
-	_nativeDeflateAsync = (data, level) => new Promise((resolve, reject) => zlib.deflateRaw(data, { level }, (err, result) => {
-		if (err) reject(err);
-		else resolve(result);
-	}));
-	_nativeCrc32 = typeof zlib.crc32 === "function" ? (data) => zlib.crc32(data) : computeCrc32;
-	if (typeof zlib.inflateRawSync === "function") _nativeInflate = (data) => zlib.inflateRawSync(data);
-} catch {}
 const hasNativeDeflate = () => _nativeDeflate !== void 0;
 const hasNativeInflate = () => _nativeInflate !== void 0;
 function wU16(b, o, v) {
