diff --git a/dist/index.cjs b/dist/index.cjs
index 27f9149be0c3925a4b80b96576ee3ca9d558093a..67e5f689de4041c0b749a536328cd430abd68d74 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -67,7 +67,11 @@ async function getDocumentProxy(data, options = {}) {
 	const { getDocument } = await getResolvedPDFJS();
 	let nodeDefaults = {};
 	if (isNode) try {
-		const base = {}.resolve("pdfjs-dist/package.json");
+		// Avoid import.meta / broken {}.resolve so CJS consumers can resolve pdfjs asset paths.
+		const { createRequire } = await import("node:module");
+		const { pathToFileURL } = await import("node:url");
+		const require = createRequire(`${process.cwd()}/`);
+		const base = pathToFileURL(require.resolve("pdfjs-dist/package.json")).href;
 		nodeDefaults = {
 			disableFontFace: true,
 			standardFontDataUrl: new URL("./standard_fonts/", base).href,
diff --git a/dist/index.mjs b/dist/index.mjs
index 6d964dcda1cfdd4333d3a832d8cbadeb8e01b693..e9fdea3dfdc78138af94cf64d31edbe9caa2705f 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -66,7 +66,11 @@ async function getDocumentProxy(data, options = {}) {
 	const { getDocument } = await getResolvedPDFJS();
 	let nodeDefaults = {};
 	if (isNode) try {
-		const base = import.meta.resolve("pdfjs-dist/package.json");
+		// Avoid import.meta.resolve so consumer CJS bundles still parse under node require.
+		const { createRequire } = await import("node:module");
+		const { pathToFileURL } = await import("node:url");
+		const require = createRequire(`${process.cwd()}/`);
+		const base = pathToFileURL(require.resolve("pdfjs-dist/package.json")).href;
 		nodeDefaults = {
 			disableFontFace: true,
 			standardFontDataUrl: new URL("./standard_fonts/", base).href,
